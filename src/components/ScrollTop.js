import ScrollToTop from 'react-scroll-to-top';

import React from 'react';

const ScrollTop = () => {
    return <ScrollToTop className="flex justify-center items-center" />;
};

export default ScrollTop;
